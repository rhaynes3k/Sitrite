<h1> Create Sitter </h1>
<strong>
  <% flash.each do |type, msg| %>
  <div id="cond">
    <%= msg %>
  </div>
<% end %>
</strong></br>
<% if @sitter.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@sitter.errors.count, "error") %> prohibited this sitter from being saved:</h2>

    <ul>
      <% @sitter.errors.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form_for (@sitter) do |s| %>
  <%= render partial: 'form', locals: {s: s} %>
<% end %>
